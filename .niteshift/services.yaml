services:
  - name: postgres
    command: docker compose -f docker-compose.yml -f .niteshift/files/docker-compose.db.yml up db
    stopGracePeriod: 30s
  - name: web
    command: pnpm dev
    port: 3000
    auth:
      type: cookie-file
      command: .niteshift/files/auth-login.mjs
